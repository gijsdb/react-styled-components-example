import React from 'react';
import { Wrapper, Header, Button} from './StyledComponents/styles'

function App() {
  return (
  <div>
    <Header>Styled Components Example</Header>
    <Button>Sweet as</Button>
    <Button primary>Primary button</Button>
  </div>
  );
}

export default App;
